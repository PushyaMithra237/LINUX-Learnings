/ - ROOT DIRECTORY
/home/ - STORES ALL PERSONAL FILES
echo$HOME - SHOWS THE HOME DIRECTORY(whatever comes after echo is an argument)
git- FILE COMMAND
mkdir - CREATES A NEW DIRECTORY
mkdir -p -- CREATES MULTIPLE DIRECTORIES IN ONE GO 
mv directory resdirectory --- MOVES FILE FROM ONE DIRECTORY TO ANOTHER
mv --- CAN ALSO BE USED TO RENAME DIRECTORIES 
rm - r -- RECURSIVELY REMOVE CONTENTS OF WHATEVER IS GIVEN 
uname - r --- TO GET THE EXACT VERSION OF THE CURRENT KERNAL
dmesg -- TO PRINT MSGS GENERATED BY THE KERNAL
lsblk -- TO COUNT THE NUMBER OF BLOCK DEVICES OF THE TYPE DISK
sudo -- ALLOWS USER TO RUN PROGRAMS WITH SECURITY PRIVELEGES
get-default --WILL GET YOU THE DEFAULT SYSTEMD TARGET
set-default -- can change the default user
sudo file (location) --TO CHECK WHAT TYPE OF FILE IT IS 
/opt --- TO INSTALL THIRD PARTY IDE DIRECTORY RECOMMEND FOR IT
lshw -- shows in depth reports of the system
pwd - CURRENT WORKING DIRECTORY CHECK
ls -- list everything
ls -a -- list everything including the hidden fils
cd -- move to  a folder in that directory
df -h -- to check for disk space in human readable form
free -m --- to check how RAM IS FREE
tail -n 20 file_name -- to read the last 20 lines in a file
tail -f -- to watch it live for new entries
touch -- for creating new files
echo -- to wrie something into a file
cp (curr location) to (new location)-- for copying the file

ps aux -- to list all the running process
ps aux | grep bash -- grep for filtering among the process that you care about 
top --- top process consuming data (pressing 1 while the command is running will give individual CPU utilization)
htop--- more user friendly (color coded version) way of showing the top consuming processes can kill any process from the interface itself 
mpstat -- displays statistics for each available processor 
mpstat 1 5 -- gets 5 reports at 1 second interval 
vmstat -- Displays virtual memory statistics along with CPU activity(user,idle,wait time)
iostat -c --provides concise summary of CPU utilization
sar --- collect system information over time
ps --- used to check which process is consuming more CPU
chmod-- commad to give permissions
chmod u+x

grep 'error' location -- gives the location of all the files with error
grep 'error' i -- case insensitivity 
grep 'error' ic -- count howmy errors
ip addr show --  to show the current ip address of the machine
hostname -I -- jsut prints the ip 
mpstat -P ALL---
uptime-- gives load averages






